
# scratch commit-message file (git -F cannot read an absolute path here:
# the apostrophe in "Paul's PC2" breaks git.exe, so it is written locally)
.commitmsg.tmp

# 🚨 THE REVIEW QUEUE IS LOCAL ONLY AND MUST NEVER DEPLOY.
# Pages publishes this repo's ROOT, so a committed review/ would be a live URL
# showing unfinished work and the questions I still owe Paul. Generated by
# tools/build-review.js from tools/review-queue.json, which IS committed so the
# next session knows what is still pending.
review/
